#include "spec.h"
#include <stdbool.h>

char* showElement(TYPE* element){
  /*
    To adapt to user defined type:
      Please implement a function that prints elements of list.
  */
  /*
  Example:
    char* buf = (char*)malloc(sizeof(char) * 20);
    sprintf(buf, "%s", element.value);
    return buf;
  */
  return "";//dummy
}

bool compareElement(TYPE fstArg, TYPE sndArg){
  /*
    To adapt to user defined type:
      Please implement a function that compares the elements and key of the list.
  */
  /*
    Example:
      if(fstarg.value == sndArg.value)
        return true;
      else
        return false;
  */
  return true;//dummy
}
